import LoadingSpinner from "../../LoadingSpinner";

const UserActiveModalSkeleton = () => {
  return (
    <div className="h-40 w-full">
      <LoadingSpinner />
    </div>
  );
};

export default UserActiveModalSkeleton;
