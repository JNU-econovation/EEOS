spring:
  config:
    activate:
      on-profile: test-token

security:
  jwt:
    access:
      validTime: 36000
      secretKey: accesstokensecretkeyaccesstokensecretkey
    refresh:
      validTime: 36000
      secretKey: refreshtokensecretkeyrefreshtokensecretkey