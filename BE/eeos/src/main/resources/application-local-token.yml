spring:
  config:
    activate:
      on-profile: local-token

security:
  jwt:
    token:
      secretKey: asccesssecretkeyoverflowsecrekey
      access:
        validTime: 1800000
      refresh:
        validTime: 3600000